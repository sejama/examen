{% extends 'base.html.twig' %}

{% block title %}Provincia{% endblock %}

{% block body %}
    <h1>Provincia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ provincia.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ provincia.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('app_provincia_index') }}" role="button">Volver</a>
    <a class="btn btn-primary" href="{{ path('app_provincia_edit', {'id': provincia.id}) }}" role="button">Editar</a>
    {{ include('provincia/_delete_form.html.twig') }}
{% endblock %}
